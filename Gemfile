source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in rails-ci.gemspec
gemspec

# Used to output detailed results when using minitest
gem 'minitest-reporters'